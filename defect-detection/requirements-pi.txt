# Raspberry Pi 5 runtime (aarch64 / Bookworm)
#
# Step 1 — system packages:
#   sudo apt install -y python3-picamera2 python3-dev \
#       libatlas-base-dev libjpeg-dev python3-lgpio lgpio
#
# Step 2 — venv (inherit apt packages for picamera2/libcamera bindings):
#   python3 -m venv --system-site-packages venv
#   source venv/bin/activate
#   pip install -r requirements-pi.txt

numpy
opencv-python
Pillow
flask
python-dotenv
gpiozero
tflite-runtime

# NOT included — incompatible with Pi 5 (RP1 chip):
# RPi.GPIO
#
# NOT included — training only, run on PC/Colab:
# tensorflow, torch, onnx
