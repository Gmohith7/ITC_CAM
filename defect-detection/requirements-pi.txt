# Raspberry Pi 5 runtime (aarch64 / Bookworm)
#
# Step 1 — system packages:
#   sudo apt install -y python3-picamera2 python3-dev \
#       python3-lgpio lgpio \
#       libatlas-base-dev libjpeg-dev
#
# Step 2 — venv (--system-site-packages exposes picamera2/libcamera to the venv):
#   python3 -m venv --system-site-packages venv
#   source venv/bin/activate
#   pip install -r requirements-pi.txt

numpy
opencv-python
Pillow
flask
python-dotenv
rapidocr_onnxruntime    # OCR engine (PP-OCR on onnxruntime; reliable on ARM)
gpiozero

# RPi.GPIO is NOT listed — incompatible with Pi 5 (RP1 chip).
